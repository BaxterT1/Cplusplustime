#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


/*int main() { // This one was not accepted but works.
    int a{}, b{}, c{};

    int sum{};

    std::cout << "Enter three numbers seperated by spaces: "; std:cin >> a >> b >> c;

    sum = a + b + c;

    std::cout << "You entered the numbers: " << sum << '\n';
    return 0;
}
*/


int main() { // this one was correct
    int a{}, b{}, c{};

    int sum{};

    std:cin >> a >> b >> c;

    sum = a + b + c;

    std::cout << sum << '\n';

    return 0;
}
